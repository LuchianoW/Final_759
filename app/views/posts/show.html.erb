<h2>帖子详情</h2>

<p><strong>用户:</strong> <%= link_to @post.user.username, user_path(@post.user) %></p>

<p><strong>内容:</strong> <%= @post.content %></p>
<p><strong>发布时间:</strong> <%= @post.created_at.strftime("%Y-%m-%d %H:%M:%S") %></p>

<% if @post.user == current_user %>
  <%= link_to '删除帖子', post_path(@post), method: :delete, data: { confirm: '确定要删除这条帖子吗？', turbo_method: :delete } %>
<% end %>


<%= link_to '返回主页', posts_path %>
